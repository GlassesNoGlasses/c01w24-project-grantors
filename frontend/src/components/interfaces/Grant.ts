export interface Grant {
    id: Number,
    title: String,
    description: String,
    posted: Date,
    deadline: Date,
    amount: Number,
    organization: String,
    category: String,
    contact: String,
}